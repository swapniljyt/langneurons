# ══════════════════════════════════════════════════════════════════
# LangNeurons — Full Dependency List
# ══════════════════════════════════════════════════════════════════
# Install:  pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════


# ── Core LangChain / LangGraph stack ──────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.3.0


# ── LLM Providers (install only those you use) ────────────────────

# OpenRouter  → access 100+ models (Claude, GPT, Gemini, GLM, etc.)
#   .env: LLM_PROVIDER=openrouter  OPENROUTER_API_KEY=sk-or-...
langchain-openrouter>=0.1.0

# Moonshot Kimi
#   .env: LLM_PROVIDER=moonshot  MOONSHOT_API_KEY=sk-...
langchain-moonshot>=0.1.0

# Anthropic Claude (direct)
#   .env: LLM_PROVIDER=anthropic  ANTHROPIC_API_KEY=sk-ant-...
langchain-anthropic>=0.3.0

# OpenAI / GPT
#   .env: LLM_PROVIDER=openai  OPENAI_API_KEY=sk-proj-...
langchain-openai>=0.3.0

# Google Gemini
#   .env: LLM_PROVIDER=google  GEMINI_API=AIza...
langchain-google-genai>=2.0.0
google-genai>=1.0.0

# Amazon Bedrock
#   .env: LLM_PROVIDER=bedrock  AWS_ACCESS_KEY_ID=... AWS_SECRET_ACCESS_KEY=... AWS_DEFAULT_REGION=...
langchain-aws>=1.5.0
boto3>=1.43.0

# Groq (optional fast inference)
#   .env: LLM_PROVIDER=groq  GROQ_API_KEY=gsk_...
# langchain-groq>=0.3.0


# ── Web Search tool ───────────────────────────────────────────────
langchain-tavily>=0.2.0


# ── Redis (agent memory + session persistence) ────────────────────
redis>=5.0.0


# ── Terminal UI ───────────────────────────────────────────────────
rich>=13.7.0


# ── Data validation ───────────────────────────────────────────────
pydantic>=2.7.0


# ── Token counting ────────────────────────────────────────────────
tiktoken>=0.7.0


# ── Environment variable loading ──────────────────────────────────
python-dotenv>=1.0.0


# ── Retry logic ───────────────────────────────────────────────────
tenacity>=8.5.0


# ── Document parsing tools ────────────────────────────────────────
PyPDF2>=3.0.0
python-docx>=1.1.0


# ── PDF generation (resume/report export) ────────────────────────
fpdf2>=2.7.0


# ── Markdown rendering ────────────────────────────────────────────
markdown>=3.6


# ── HTTP client (used by OpenRouter, Tavily, etc.) ────────────────
httpx>=0.27.0


# ── Web Console / FastAPI Server ──────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
websockets>=12.0
pymysql>=1.1.1

